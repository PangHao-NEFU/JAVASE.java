可变参数又称参数个数可变，用作方法的形参出现,
符号：...
相当于把参数都存放在数组a中了
public static int sum(int... a){}
如果一个函数需要可变参数和不可变参数，要把可变参数写在不可变参数后面

Arrays工具类中有一个静态方法
List<String> list=Arrays.asList("a","b","c");
list是固定大小的列表，在创建时就决定了它的大小不可更改，不可增删元素，但可以修改元素


List接口中有一个静态方法
List<String> list=List.of("a","b","c");
返回一个不可变列表，不可增删改

Set接口中有一个静态方法
Set<String> list=Set.of("a","b","c");不可以接受相同参数
返回一个不可变集合，没有重复元素，不可修改





